# {Project Name} — Quick Start Recipes

> **Atlas Map: Trails to Follow**
> Copy-paste recipes for common tasks.
> The blueprint generator reads this to produce consistent action items.

## Common Tasks — Copy-Paste Ready

### Add a New {Endpoint / Route / Page}

```bash
# 1. {Create file}
{path}

# 2. {Register / import}
{path} → add: {import statement}

# 3. {Add supporting file}
{path}

# 4. {Migration or build step}
{command}
```

### Add a New {Service / Feature}

```bash
# 1. {First step}
{path or command}

# 2. {Second step}
{path or command}

# 3. {Third step}
{path or command}
```

### Add a New {Integration / Channel / Provider}

```bash
# 1. {First step}
{details}

# 2. {Second step}
{details}
```

## Quick Commands

```bash
# Start dev server
{command}

# Create migration
{command}

# Apply migrations
{command}

# Compile / type check
{command}

# Run tests
{command}

# Run specific test
{command}
```

## Environment Quick Setup

```bash
# Activate venv / install deps
{command}

# Install new dependency
{command}
```
